# Digit Detection with Deep Learning

This is a full example of how to detect hand-writting number with deep learning.

## Run

```bash
python -m venv Env
./Env/Scripts/activate
pip install -r requirements.txt
```
